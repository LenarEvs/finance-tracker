import { PageShell } from "../components/layout/PageShell";

export function AuditLog() {
  return (
    <PageShell>
      <h1>Audit Log</h1>
      <p>Paginated table, filters, before/after diff modal — not implemented</p>
    </PageShell>
  );
}
