import { PageShell } from "../components/layout/PageShell";

export function ImportExport() {
  return (
    <PageShell>
      <h1>Import / Export</h1>
      <p>CSV export button, file upload, preview table — not implemented</p>
    </PageShell>
  );
}
